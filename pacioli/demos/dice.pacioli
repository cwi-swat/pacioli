import "pacioli/primitives.schema";

define dice = [1,2,3,4,5,6];
define sums = [x+y | x in list dice, y in list dice];
define amount = listSize(sums);

define cnt(n) = count[s | s in list sums, s=n];

{tuple(i, cnt(i)/amount) | i in set {x+y | x in list dice, y in list dice}}