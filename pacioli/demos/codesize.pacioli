let agg_matrix = owner.parent*,
    agg(x) = x.agg_matrix,
    modules = columnDomain(owner),
    bytesPerLine = agg(fileSize)/agg(lines)
in
    {tuple(m, get(bytesPerLine, empty, m)) | m in modules} 
end